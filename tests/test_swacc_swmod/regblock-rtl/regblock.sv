// Generated by PeakRDL-regblock - A free and open-source SystemVerilog generator
//  https://github.com/SystemRDL/PeakRDL-regblock

module regblock (
        input wire clk,
        input wire rst,

        input wire s_apb_psel,
        input wire s_apb_penable,
        input wire s_apb_pwrite,
        input wire [2:0] s_apb_pprot,
        input wire [2:0] s_apb_paddr,
        input wire [7:0] s_apb_pwdata,
        input wire [0:0] s_apb_pstrb,
        output logic s_apb_pready,
        output logic [7:0] s_apb_prdata,
        output logic s_apb_pslverr,

        input regblock_pkg::regblock__in_t hwif_in,
        output regblock_pkg::regblock__out_t hwif_out
    );

    //--------------------------------------------------------------------------
    // CPU Bus interface logic
    //--------------------------------------------------------------------------
    logic cpuif_req;
    logic cpuif_req_is_wr;
    logic [2:0] cpuif_addr;
    logic [7:0] cpuif_wr_data;
    logic [7:0] cpuif_wr_biten;
    logic cpuif_req_stall_wr;
    logic cpuif_req_stall_rd;

    logic cpuif_rd_ack;
    logic cpuif_rd_err;
    logic [7:0] cpuif_rd_data;

    logic cpuif_wr_ack;
    logic cpuif_wr_err;

    // Request
    logic is_active;
    always_ff @(posedge clk) begin
        if(rst) begin
            is_active <= '0;
            cpuif_req <= '0;
            cpuif_req_is_wr <= '0;
            cpuif_addr <= '0;
            cpuif_wr_data <= '0;
            cpuif_wr_biten <= '0;
        end else begin
            if(~is_active) begin
                if(s_apb_psel) begin
                    is_active <= '1;
                    cpuif_req <= '1;
                    cpuif_req_is_wr <= s_apb_pwrite;
                    cpuif_addr <= s_apb_paddr[2:0];
                    cpuif_wr_data <= s_apb_pwdata;
                    for(int i=0; i<1; i++) begin
                        cpuif_wr_biten[i*8 +: 8] <= {8{s_apb_pstrb[i]}};
                    end
                end
            end else begin
                cpuif_req <= '0;
                if(cpuif_rd_ack || cpuif_wr_ack) begin
                    is_active <= '0;
                end
            end
        end
    end

    // Response
    assign s_apb_pready = cpuif_rd_ack | cpuif_wr_ack;
    assign s_apb_prdata = cpuif_rd_data;
    assign s_apb_pslverr = cpuif_rd_err | cpuif_wr_err;

    logic cpuif_req_masked;

    // Read & write latencies are balanced. Stalls not required
    assign cpuif_req_stall_rd = '0;
    assign cpuif_req_stall_wr = '0;
    assign cpuif_req_masked = cpuif_req
                            & !(!cpuif_req_is_wr & cpuif_req_stall_rd)
                            & !(cpuif_req_is_wr & cpuif_req_stall_wr);

    //--------------------------------------------------------------------------
    // Address Decode
    //--------------------------------------------------------------------------
    typedef struct {
        logic r1;
        logic r2;
        logic r3;
        logic r4;
        logic r5;
    } decoded_reg_strb_t;
    decoded_reg_strb_t decoded_reg_strb;
    logic decoded_err;
    logic decoded_req;
    logic decoded_req_is_wr;
    logic [7:0] decoded_wr_data;
    logic [7:0] decoded_wr_biten;

    always_comb begin
        automatic logic is_valid_addr;
        automatic logic is_invalid_rw;
        is_valid_addr = '1; // No error checking on valid address access
        is_invalid_rw = '0;
        decoded_reg_strb.r1 = cpuif_req_masked & (cpuif_addr == 3'h0) & !cpuif_req_is_wr;
        decoded_reg_strb.r2 = cpuif_req_masked & (cpuif_addr == 3'h1);
        decoded_reg_strb.r3 = cpuif_req_masked & (cpuif_addr == 3'h2);
        decoded_reg_strb.r4 = cpuif_req_masked & (cpuif_addr == 3'h3);
        decoded_reg_strb.r5 = cpuif_req_masked & (cpuif_addr == 3'h4) & !cpuif_req_is_wr;
        decoded_err = (~is_valid_addr | is_invalid_rw) & decoded_req;
    end

    // Pass down signals to next stage
    assign decoded_req = cpuif_req_masked;
    assign decoded_req_is_wr = cpuif_req_is_wr;
    assign decoded_wr_data = cpuif_wr_data;
    assign decoded_wr_biten = cpuif_wr_biten;

    //--------------------------------------------------------------------------
    // Field logic
    //--------------------------------------------------------------------------
    typedef struct {
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
            } f;
        } r2;
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
            } f;
        } r3;
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
            } f;
        } r4;
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
            } f;
        } r5;
    } field_combo_t;
    field_combo_t field_combo;

    typedef struct {
        struct {
            struct {
                logic [7:0] value;
            } f;
        } r2;
        struct {
            struct {
                logic [7:0] value;
            } f;
        } r3;
        struct {
            struct {
                logic [7:0] value;
            } f;
        } r4;
        struct {
            struct {
                logic [7:0] value;
            } f;
        } r5;
    } field_storage_t;
    field_storage_t field_storage;

    assign hwif_out.r1.f.swmod = 1'b0;
    assign hwif_out.r1.f.swacc = decoded_reg_strb.r1;
    // Field: regblock.r2.f
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.r2.f.value;
        load_next_c = '0;
        if(decoded_reg_strb.r2 && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.r2.f.value & ~decoded_wr_biten[7:0]) | (decoded_wr_data[7:0] & decoded_wr_biten[7:0]);
            load_next_c = '1;
        end
        field_combo.r2.f.next = next_c;
        field_combo.r2.f.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.r2.f.value <= 8'h14;
        end else begin
            if(field_combo.r2.f.load_next) begin
                field_storage.r2.f.value <= field_combo.r2.f.next;
            end
        end
    end
    assign hwif_out.r2.f.value = field_storage.r2.f.value;
    assign hwif_out.r2.f.swmod = decoded_reg_strb.r2 && decoded_req_is_wr && |(decoded_wr_biten[7:0]);
    // Field: regblock.r3.f
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.r3.f.value;
        load_next_c = '0;
        if(decoded_reg_strb.r3 && !decoded_req_is_wr) begin // SW clear on read
            next_c = '0;
            load_next_c = '1;
        end else if(decoded_reg_strb.r3 && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.r3.f.value & ~decoded_wr_biten[7:0]) | (decoded_wr_data[7:0] & decoded_wr_biten[7:0]);
            load_next_c = '1;
        end
        field_combo.r3.f.next = next_c;
        field_combo.r3.f.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.r3.f.value <= 8'h1e;
        end else begin
            if(field_combo.r3.f.load_next) begin
                field_storage.r3.f.value <= field_combo.r3.f.next;
            end
        end
    end
    assign hwif_out.r3.f.value = field_storage.r3.f.value;
    assign hwif_out.r3.f.swmod = decoded_reg_strb.r3 && !decoded_req_is_wr || decoded_reg_strb.r3 && decoded_req_is_wr && |(decoded_wr_biten[7:0]);
    // Field: regblock.r4.f
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.r4.f.value;
        load_next_c = '0;
        if(decoded_reg_strb.r4 && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.r4.f.value & ~decoded_wr_biten[7:0]) | (decoded_wr_data[7:0] & decoded_wr_biten[7:0]);
            load_next_c = '1;
        end
        field_combo.r4.f.next = next_c;
        field_combo.r4.f.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.r4.f.value <= 8'h12;
        end else begin
            if(field_combo.r4.f.load_next) begin
                field_storage.r4.f.value <= field_combo.r4.f.next;
            end
        end
    end
    assign hwif_out.r4.f.value = field_storage.r4.f.value;
    assign hwif_out.r4.f.swmod = decoded_reg_strb.r4 && decoded_req_is_wr && |(decoded_wr_biten[7:0]);
    assign hwif_out.r4.f.swacc = decoded_reg_strb.r4;
    // Field: regblock.r5.f
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.r5.f.value;
        load_next_c = '0;
        if(decoded_reg_strb.r5 && !decoded_req_is_wr) begin // SW clear on read
            next_c = '0;
            load_next_c = '1;
        end else if(hwif_in.r5.f.we) begin // HW Write - we
            next_c = hwif_in.r5.f.next;
            load_next_c = '1;
        end
        field_combo.r5.f.next = next_c;
        field_combo.r5.f.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.r5.f.value <= 8'h1e;
        end else begin
            if(field_combo.r5.f.load_next) begin
                field_storage.r5.f.value <= field_combo.r5.f.next;
            end
        end
    end
    assign hwif_out.r5.f.value = field_storage.r5.f.value;
    assign hwif_out.r5.f.swmod = decoded_reg_strb.r5 && !decoded_req_is_wr;

    //--------------------------------------------------------------------------
    // Write response
    //--------------------------------------------------------------------------
    assign cpuif_wr_ack = decoded_req & decoded_req_is_wr;
    // Writes are always granted with no error response
    assign cpuif_wr_err = '0;

    //--------------------------------------------------------------------------
    // Readback
    //--------------------------------------------------------------------------

    logic readback_err;
    logic readback_done;
    logic [7:0] readback_data;

    // Assign readback values to a flattened array
    logic [7:0] readback_array[5];
    assign readback_array[0][7:0] = (decoded_reg_strb.r1 && !decoded_req_is_wr) ? hwif_in.r1.f.next : '0;
    assign readback_array[1][7:0] = (decoded_reg_strb.r2 && !decoded_req_is_wr) ? field_storage.r2.f.value : '0;
    assign readback_array[2][7:0] = (decoded_reg_strb.r3 && !decoded_req_is_wr) ? field_storage.r3.f.value : '0;
    assign readback_array[3][7:0] = (decoded_reg_strb.r4 && !decoded_req_is_wr) ? field_storage.r4.f.value : '0;
    assign readback_array[4][7:0] = (decoded_reg_strb.r5 && !decoded_req_is_wr) ? field_storage.r5.f.value : '0;

    // Reduce the array
    always_comb begin
        automatic logic [7:0] readback_data_var;
        readback_done = decoded_req & ~decoded_req_is_wr;
        readback_err = '0;
        readback_data_var = '0;
        for(int i=0; i<5; i++) readback_data_var |= readback_array[i];
        readback_data = readback_data_var;
    end

    assign cpuif_rd_ack = readback_done;
    assign cpuif_rd_data = readback_data;
    assign cpuif_rd_err = readback_err;
endmodule
