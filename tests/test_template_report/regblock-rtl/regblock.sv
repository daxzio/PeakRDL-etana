// Generated by PeakRDL-regblock - A free and open-source SystemVerilog generator
//  https://github.com/SystemRDL/PeakRDL-regblock

module regblock (
        input wire clk,
        input wire rst,

        input wire s_apb_psel,
        input wire s_apb_penable,
        input wire s_apb_pwrite,
        input wire [2:0] s_apb_pprot,
        input wire [3:0] s_apb_paddr,
        input wire [31:0] s_apb_pwdata,
        input wire [3:0] s_apb_pstrb,
        output logic s_apb_pready,
        output logic [31:0] s_apb_prdata,
        output logic s_apb_pslverr,

        input regblock_pkg::regblock__in_t hwif_in,
        output regblock_pkg::regblock__out_t hwif_out
    );

    //--------------------------------------------------------------------------
    // CPU Bus interface logic
    //--------------------------------------------------------------------------
    logic cpuif_req;
    logic cpuif_req_is_wr;
    logic [3:0] cpuif_addr;
    logic [31:0] cpuif_wr_data;
    logic [31:0] cpuif_wr_biten;
    logic cpuif_req_stall_wr;
    logic cpuif_req_stall_rd;

    logic cpuif_rd_ack;
    logic cpuif_rd_err;
    logic [31:0] cpuif_rd_data;

    logic cpuif_wr_ack;
    logic cpuif_wr_err;

    // Request
    logic is_active;
    always_ff @(posedge clk) begin
        if(rst) begin
            is_active <= '0;
            cpuif_req <= '0;
            cpuif_req_is_wr <= '0;
            cpuif_addr <= '0;
            cpuif_wr_data <= '0;
            cpuif_wr_biten <= '0;
        end else begin
            if(~is_active) begin
                if(s_apb_psel) begin
                    is_active <= '1;
                    cpuif_req <= '1;
                    cpuif_req_is_wr <= s_apb_pwrite;
                    cpuif_addr <= {s_apb_paddr[3:2], 2'b0};
                    cpuif_wr_data <= s_apb_pwdata;
                    for(int i=0; i<4; i++) begin
                        cpuif_wr_biten[i*8 +: 8] <= {8{s_apb_pstrb[i]}};
                    end
                end
            end else begin
                cpuif_req <= '0;
                if(cpuif_rd_ack || cpuif_wr_ack) begin
                    is_active <= '0;
                end
            end
        end
    end

    // Response
    assign s_apb_pready = cpuif_rd_ack | cpuif_wr_ack;
    assign s_apb_prdata = cpuif_rd_data;
    assign s_apb_pslverr = cpuif_rd_err | cpuif_wr_err;

    logic cpuif_req_masked;

    // Read & write latencies are balanced. Stalls not required
    assign cpuif_req_stall_rd = '0;
    assign cpuif_req_stall_wr = '0;
    assign cpuif_req_masked = cpuif_req
                            & !(!cpuif_req_is_wr & cpuif_req_stall_rd)
                            & !(cpuif_req_is_wr & cpuif_req_stall_wr);

    //--------------------------------------------------------------------------
    // Address Decode
    //--------------------------------------------------------------------------
    typedef struct {
        logic STATUS;
        logic CONTROL;
        logic DATA;
    } decoded_reg_strb_t;
    decoded_reg_strb_t decoded_reg_strb;
    logic decoded_req;
    logic decoded_req_is_wr;
    logic [31:0] decoded_wr_data;
    logic [31:0] decoded_wr_biten;

    always_comb begin
        decoded_reg_strb.STATUS = cpuif_req_masked & (cpuif_addr == 4'h0);
        decoded_reg_strb.CONTROL = cpuif_req_masked & (cpuif_addr == 4'h4);
        decoded_reg_strb.DATA = cpuif_req_masked & (cpuif_addr == 4'h8);
    end

    // Pass down signals to next stage
    assign decoded_req = cpuif_req_masked;
    assign decoded_req_is_wr = cpuif_req_is_wr;
    assign decoded_wr_data = cpuif_wr_data;
    assign decoded_wr_biten = cpuif_wr_biten;

    //--------------------------------------------------------------------------
    // Field logic
    //--------------------------------------------------------------------------
    typedef struct {
        struct {
            struct {
                logic next;
                logic load_next;
            } enable;
            struct {
                logic [2:0] next;
                logic load_next;
            } mode;
        } CONTROL;
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
            } value;
        } DATA;
    } field_combo_t;
    field_combo_t field_combo;

    typedef struct {
        struct {
            struct {
                logic value;
            } enable;
            struct {
                logic [2:0] value;
            } mode;
        } CONTROL;
        struct {
            struct {
                logic [7:0] value;
            } value;
        } DATA;
    } field_storage_t;
    field_storage_t field_storage;

    // Field: regblock.CONTROL.enable
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.CONTROL.enable.value;
        load_next_c = '0;
        if(decoded_reg_strb.CONTROL && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.CONTROL.enable.value & ~decoded_wr_biten[0:0]) | (decoded_wr_data[0:0] & decoded_wr_biten[0:0]);
            load_next_c = '1;
        end
        field_combo.CONTROL.enable.next = next_c;
        field_combo.CONTROL.enable.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(field_combo.CONTROL.enable.load_next) begin
            field_storage.CONTROL.enable.value <= field_combo.CONTROL.enable.next;
        end
    end
    assign hwif_out.CONTROL.enable.value = field_storage.CONTROL.enable.value;
    // Field: regblock.CONTROL.mode
    always_comb begin
        automatic logic [2:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.CONTROL.mode.value;
        load_next_c = '0;
        if(decoded_reg_strb.CONTROL && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.CONTROL.mode.value & ~decoded_wr_biten[3:1]) | (decoded_wr_data[3:1] & decoded_wr_biten[3:1]);
            load_next_c = '1;
        end
        field_combo.CONTROL.mode.next = next_c;
        field_combo.CONTROL.mode.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(field_combo.CONTROL.mode.load_next) begin
            field_storage.CONTROL.mode.value <= field_combo.CONTROL.mode.next;
        end
    end
    assign hwif_out.CONTROL.mode.value = field_storage.CONTROL.mode.value;
    // Field: regblock.DATA.value
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.DATA.value.value;
        load_next_c = '0;
        if(decoded_reg_strb.DATA && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.DATA.value.value & ~decoded_wr_biten[7:0]) | (decoded_wr_data[7:0] & decoded_wr_biten[7:0]);
            load_next_c = '1;
        end else begin // HW Write
            next_c = hwif_in.DATA.value.next;
            load_next_c = '1;
        end
        field_combo.DATA.value.next = next_c;
        field_combo.DATA.value.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(field_combo.DATA.value.load_next) begin
            field_storage.DATA.value.value <= field_combo.DATA.value.next;
        end
    end
    assign hwif_out.DATA.value.value = field_storage.DATA.value.value;

    //--------------------------------------------------------------------------
    // Write response
    //--------------------------------------------------------------------------
    assign cpuif_wr_ack = decoded_req & decoded_req_is_wr;
    // Writes are always granted with no error response
    assign cpuif_wr_err = '0;

    //--------------------------------------------------------------------------
    // Readback
    //--------------------------------------------------------------------------

    logic readback_err;
    logic readback_done;
    logic [31:0] readback_data;

    // Assign readback values to a flattened array
    logic [31:0] readback_array[3];
    assign readback_array[0][0:0] = (decoded_reg_strb.STATUS && !decoded_req_is_wr) ? hwif_in.STATUS.ready.next : '0;
    assign readback_array[0][1:1] = (decoded_reg_strb.STATUS && !decoded_req_is_wr) ? hwif_in.STATUS.error.next : '0;
    assign readback_array[0][7:2] = '0;
    assign readback_array[0][15:8] = (decoded_reg_strb.STATUS && !decoded_req_is_wr) ? hwif_in.STATUS.status.next : '0;
    assign readback_array[0][31:16] = '0;
    assign readback_array[1][0:0] = (decoded_reg_strb.CONTROL && !decoded_req_is_wr) ? field_storage.CONTROL.enable.value : '0;
    assign readback_array[1][3:1] = (decoded_reg_strb.CONTROL && !decoded_req_is_wr) ? field_storage.CONTROL.mode.value : '0;
    assign readback_array[1][31:4] = '0;
    assign readback_array[2][7:0] = (decoded_reg_strb.DATA && !decoded_req_is_wr) ? field_storage.DATA.value.value : '0;
    assign readback_array[2][31:8] = '0;

    // Reduce the array
    always_comb begin
        automatic logic [31:0] readback_data_var;
        readback_done = decoded_req & ~decoded_req_is_wr;
        readback_err = '0;
        readback_data_var = '0;
        for(int i=0; i<3; i++) readback_data_var |= readback_array[i];
        readback_data = readback_data_var;
    end

    assign cpuif_rd_ack = readback_done;
    assign cpuif_rd_data = readback_data;
    assign cpuif_rd_err = readback_err;
endmodule
