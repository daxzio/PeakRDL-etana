[pytest]
# Pytest configuration for PeakRDL-etana tests
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --verbose --tb=short

# Coverage options (when using pytest --cov)
# Note: Most tests use cocotb, coverage tracked via coverage.py directly
