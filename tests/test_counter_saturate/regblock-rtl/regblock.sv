// Generated by PeakRDL-regblock - A free and open-source SystemVerilog generator
//  https://github.com/SystemRDL/PeakRDL-regblock

module regblock (
        input wire clk,
        input wire rst,

        input wire s_apb_psel,
        input wire s_apb_penable,
        input wire s_apb_pwrite,
        input wire [2:0] s_apb_pprot,
        input wire [3:0] s_apb_paddr,
        input wire [31:0] s_apb_pwdata,
        input wire [3:0] s_apb_pstrb,
        output logic s_apb_pready,
        output logic [31:0] s_apb_prdata,
        output logic s_apb_pslverr,

        output regblock_pkg::regblock__out_t hwif_out
    );

    //--------------------------------------------------------------------------
    // CPU Bus interface logic
    //--------------------------------------------------------------------------
    logic cpuif_req;
    logic cpuif_req_is_wr;
    logic [3:0] cpuif_addr;
    logic [31:0] cpuif_wr_data;
    logic [31:0] cpuif_wr_biten;
    logic cpuif_req_stall_wr;
    logic cpuif_req_stall_rd;

    logic cpuif_rd_ack;
    logic cpuif_rd_err;
    logic [31:0] cpuif_rd_data;

    logic cpuif_wr_ack;
    logic cpuif_wr_err;

    // Request
    logic is_active;
    always_ff @(posedge clk) begin
        if(rst) begin
            is_active <= '0;
            cpuif_req <= '0;
            cpuif_req_is_wr <= '0;
            cpuif_addr <= '0;
            cpuif_wr_data <= '0;
            cpuif_wr_biten <= '0;
        end else begin
            if(~is_active) begin
                if(s_apb_psel) begin
                    is_active <= '1;
                    cpuif_req <= '1;
                    cpuif_req_is_wr <= s_apb_pwrite;
                    cpuif_addr <= {s_apb_paddr[3:2], 2'b0};
                    cpuif_wr_data <= s_apb_pwdata;
                    for(int i=0; i<4; i++) begin
                        cpuif_wr_biten[i*8 +: 8] <= {8{s_apb_pstrb[i]}};
                    end
                end
            end else begin
                cpuif_req <= '0;
                if(cpuif_rd_ack || cpuif_wr_ack) begin
                    is_active <= '0;
                end
            end
        end
    end

    // Response
    assign s_apb_pready = cpuif_rd_ack | cpuif_wr_ack;
    assign s_apb_prdata = cpuif_rd_data;
    assign s_apb_pslverr = cpuif_rd_err | cpuif_wr_err;

    logic cpuif_req_masked;

    // Read & write latencies are balanced. Stalls not required
    assign cpuif_req_stall_rd = '0;
    assign cpuif_req_stall_wr = '0;
    assign cpuif_req_masked = cpuif_req
                            & !(!cpuif_req_is_wr & cpuif_req_stall_rd)
                            & !(cpuif_req_is_wr & cpuif_req_stall_wr);

    //--------------------------------------------------------------------------
    // Address Decode
    //--------------------------------------------------------------------------
    typedef struct {
        logic saturate_via_bool;
        logic saturate_via_const;
        logic saturate_via_ref;
        logic saturate_control;
    } decoded_reg_strb_t;
    decoded_reg_strb_t decoded_reg_strb;
    logic decoded_req;
    logic decoded_req_is_wr;
    logic [31:0] decoded_wr_data;
    logic [31:0] decoded_wr_biten;

    always_comb begin
        decoded_reg_strb.saturate_via_bool = cpuif_req_masked & (cpuif_addr == 4'h0);
        decoded_reg_strb.saturate_via_const = cpuif_req_masked & (cpuif_addr == 4'h4);
        decoded_reg_strb.saturate_via_ref = cpuif_req_masked & (cpuif_addr == 4'h8);
        decoded_reg_strb.saturate_control = cpuif_req_masked & (cpuif_addr == 4'hc);
    end

    // Pass down signals to next stage
    assign decoded_req = cpuif_req_masked;
    assign decoded_req_is_wr = cpuif_req_is_wr;
    assign decoded_wr_data = cpuif_wr_data;
    assign decoded_wr_biten = cpuif_wr_biten;

    //--------------------------------------------------------------------------
    // Field logic
    //--------------------------------------------------------------------------
    typedef struct {
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
                logic incrthreshold;
                logic incrsaturate;
                logic decrthreshold;
                logic decrsaturate;
            } count;
            struct {
                logic next;
                logic load_next;
            } increment;
            struct {
                logic next;
                logic load_next;
            } decrement;
            struct {
                logic next;
                logic load_next;
            } clear;
            struct {
                logic next;
                logic load_next;
            } set;
            struct {
                logic [7:0] next;
                logic load_next;
            } step;
        } saturate_via_bool;
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
                logic incrthreshold;
                logic incrsaturate;
                logic decrthreshold;
                logic decrsaturate;
            } count;
            struct {
                logic next;
                logic load_next;
            } increment;
            struct {
                logic next;
                logic load_next;
            } decrement;
            struct {
                logic next;
                logic load_next;
            } clear;
            struct {
                logic next;
                logic load_next;
            } set;
            struct {
                logic [7:0] next;
                logic load_next;
            } step;
        } saturate_via_const;
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
                logic incrthreshold;
                logic incrsaturate;
                logic decrthreshold;
                logic decrsaturate;
            } count;
            struct {
                logic next;
                logic load_next;
            } increment;
            struct {
                logic next;
                logic load_next;
            } decrement;
            struct {
                logic next;
                logic load_next;
            } clear;
            struct {
                logic next;
                logic load_next;
            } set;
            struct {
                logic [7:0] next;
                logic load_next;
            } step;
        } saturate_via_ref;
        struct {
            struct {
                logic [7:0] next;
                logic load_next;
            } min;
            struct {
                logic [7:0] next;
                logic load_next;
            } max;
        } saturate_control;
    } field_combo_t;
    field_combo_t field_combo;

    typedef struct {
        struct {
            struct {
                logic [7:0] value;
            } count;
            struct {
                logic value;
            } increment;
            struct {
                logic value;
            } decrement;
            struct {
                logic value;
            } clear;
            struct {
                logic value;
            } set;
            struct {
                logic [7:0] value;
            } step;
        } saturate_via_bool;
        struct {
            struct {
                logic [7:0] value;
            } count;
            struct {
                logic value;
            } increment;
            struct {
                logic value;
            } decrement;
            struct {
                logic value;
            } clear;
            struct {
                logic value;
            } set;
            struct {
                logic [7:0] value;
            } step;
        } saturate_via_const;
        struct {
            struct {
                logic [7:0] value;
            } count;
            struct {
                logic value;
            } increment;
            struct {
                logic value;
            } decrement;
            struct {
                logic value;
            } clear;
            struct {
                logic value;
            } set;
            struct {
                logic [7:0] value;
            } step;
        } saturate_via_ref;
        struct {
            struct {
                logic [7:0] value;
            } min;
            struct {
                logic [7:0] value;
            } max;
        } saturate_control;
    } field_storage_t;
    field_storage_t field_storage;

    // Field: regblock.saturate_via_bool.count
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_bool.count.value;
        load_next_c = '0;
        if(field_storage.saturate_via_bool.set.value) begin // HW Set
            next_c = '1;
            load_next_c = '1;
        end else if(field_storage.saturate_via_bool.clear.value) begin // HW Clear
            next_c = '0;
            load_next_c = '1;
        end
        if(field_storage.saturate_via_bool.increment.value) begin // increment
            if(((9)'(next_c) + field_storage.saturate_via_bool.step.value) > 8'hff) begin // up-counter saturated
                next_c = 8'hff;
            end else begin
                next_c = next_c + field_storage.saturate_via_bool.step.value;
            end
            load_next_c = '1;
        end
        field_combo.saturate_via_bool.count.incrthreshold = (field_storage.saturate_via_bool.count.value >= 8'hff);
        field_combo.saturate_via_bool.count.incrsaturate = (field_storage.saturate_via_bool.count.value >= 8'hff);
        if(field_storage.saturate_via_bool.decrement.value) begin // decrement
            if((9)'(next_c) < (field_storage.saturate_via_bool.step.value + 8'd0)) begin // down-counter saturated
                next_c = 8'd0;
            end else begin
                next_c = next_c - field_storage.saturate_via_bool.step.value;
            end
            load_next_c = '1;
        end
        field_combo.saturate_via_bool.count.decrthreshold = (field_storage.saturate_via_bool.count.value <= 8'd0);
        field_combo.saturate_via_bool.count.decrsaturate = (field_storage.saturate_via_bool.count.value <= 8'd0);
        field_combo.saturate_via_bool.count.next = next_c;
        field_combo.saturate_via_bool.count.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_bool.count.value <= 8'h0;
        end else begin
            if(field_combo.saturate_via_bool.count.load_next) begin
                field_storage.saturate_via_bool.count.value <= field_combo.saturate_via_bool.count.next;
            end
        end
    end
    assign hwif_out.saturate_via_bool.count.value = field_storage.saturate_via_bool.count.value;
    // Field: regblock.saturate_via_bool.increment
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_bool.increment.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_bool && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_bool.increment.value & ~decoded_wr_biten[9:9]) | (decoded_wr_data[9:9] & decoded_wr_biten[9:9]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_bool.increment.next = next_c;
        field_combo.saturate_via_bool.increment.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_bool.increment.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_bool.increment.load_next) begin
                field_storage.saturate_via_bool.increment.value <= field_combo.saturate_via_bool.increment.next;
            end
        end
    end
    assign hwif_out.saturate_via_bool.increment.value = field_storage.saturate_via_bool.increment.value;
    // Field: regblock.saturate_via_bool.decrement
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_bool.decrement.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_bool && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_bool.decrement.value & ~decoded_wr_biten[10:10]) | (decoded_wr_data[10:10] & decoded_wr_biten[10:10]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_bool.decrement.next = next_c;
        field_combo.saturate_via_bool.decrement.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_bool.decrement.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_bool.decrement.load_next) begin
                field_storage.saturate_via_bool.decrement.value <= field_combo.saturate_via_bool.decrement.next;
            end
        end
    end
    assign hwif_out.saturate_via_bool.decrement.value = field_storage.saturate_via_bool.decrement.value;
    // Field: regblock.saturate_via_bool.clear
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_bool.clear.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_bool && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_bool.clear.value & ~decoded_wr_biten[11:11]) | (decoded_wr_data[11:11] & decoded_wr_biten[11:11]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_bool.clear.next = next_c;
        field_combo.saturate_via_bool.clear.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_bool.clear.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_bool.clear.load_next) begin
                field_storage.saturate_via_bool.clear.value <= field_combo.saturate_via_bool.clear.next;
            end
        end
    end
    assign hwif_out.saturate_via_bool.clear.value = field_storage.saturate_via_bool.clear.value;
    // Field: regblock.saturate_via_bool.set
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_bool.set.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_bool && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_bool.set.value & ~decoded_wr_biten[12:12]) | (decoded_wr_data[12:12] & decoded_wr_biten[12:12]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_bool.set.next = next_c;
        field_combo.saturate_via_bool.set.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_bool.set.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_bool.set.load_next) begin
                field_storage.saturate_via_bool.set.value <= field_combo.saturate_via_bool.set.next;
            end
        end
    end
    assign hwif_out.saturate_via_bool.set.value = field_storage.saturate_via_bool.set.value;
    // Field: regblock.saturate_via_bool.step
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_bool.step.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_bool && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_bool.step.value & ~decoded_wr_biten[23:16]) | (decoded_wr_data[23:16] & decoded_wr_biten[23:16]);
            load_next_c = '1;
        end
        field_combo.saturate_via_bool.step.next = next_c;
        field_combo.saturate_via_bool.step.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_bool.step.value <= 8'h1;
        end else begin
            if(field_combo.saturate_via_bool.step.load_next) begin
                field_storage.saturate_via_bool.step.value <= field_combo.saturate_via_bool.step.next;
            end
        end
    end
    // Field: regblock.saturate_via_const.count
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_const.count.value;
        load_next_c = '0;
        if(field_storage.saturate_via_const.set.value) begin // HW Set
            next_c = '1;
            load_next_c = '1;
        end else if(field_storage.saturate_via_const.clear.value) begin // HW Clear
            next_c = '0;
            load_next_c = '1;
        end
        if(field_storage.saturate_via_const.increment.value) begin // increment
            if(((9)'(next_c) + field_storage.saturate_via_const.step.value) > 8'hfa) begin // up-counter saturated
                next_c = 8'hfa;
            end else begin
                next_c = next_c + field_storage.saturate_via_const.step.value;
            end
            load_next_c = '1;
        end
        field_combo.saturate_via_const.count.incrthreshold = (field_storage.saturate_via_const.count.value >= 8'hff);
        field_combo.saturate_via_const.count.incrsaturate = (field_storage.saturate_via_const.count.value >= 8'hfa);
        if(field_storage.saturate_via_const.decrement.value) begin // decrement
            if((9)'(next_c) < (field_storage.saturate_via_const.step.value + 8'h5)) begin // down-counter saturated
                next_c = 8'h5;
            end else begin
                next_c = next_c - field_storage.saturate_via_const.step.value;
            end
            load_next_c = '1;
        end
        field_combo.saturate_via_const.count.decrthreshold = (field_storage.saturate_via_const.count.value <= 8'd0);
        field_combo.saturate_via_const.count.decrsaturate = (field_storage.saturate_via_const.count.value <= 8'h5);
        field_combo.saturate_via_const.count.next = next_c;
        field_combo.saturate_via_const.count.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_const.count.value <= 8'h0;
        end else begin
            if(field_combo.saturate_via_const.count.load_next) begin
                field_storage.saturate_via_const.count.value <= field_combo.saturate_via_const.count.next;
            end
        end
    end
    assign hwif_out.saturate_via_const.count.value = field_storage.saturate_via_const.count.value;
    // Field: regblock.saturate_via_const.increment
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_const.increment.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_const && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_const.increment.value & ~decoded_wr_biten[9:9]) | (decoded_wr_data[9:9] & decoded_wr_biten[9:9]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_const.increment.next = next_c;
        field_combo.saturate_via_const.increment.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_const.increment.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_const.increment.load_next) begin
                field_storage.saturate_via_const.increment.value <= field_combo.saturate_via_const.increment.next;
            end
        end
    end
    assign hwif_out.saturate_via_const.increment.value = field_storage.saturate_via_const.increment.value;
    // Field: regblock.saturate_via_const.decrement
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_const.decrement.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_const && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_const.decrement.value & ~decoded_wr_biten[10:10]) | (decoded_wr_data[10:10] & decoded_wr_biten[10:10]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_const.decrement.next = next_c;
        field_combo.saturate_via_const.decrement.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_const.decrement.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_const.decrement.load_next) begin
                field_storage.saturate_via_const.decrement.value <= field_combo.saturate_via_const.decrement.next;
            end
        end
    end
    assign hwif_out.saturate_via_const.decrement.value = field_storage.saturate_via_const.decrement.value;
    // Field: regblock.saturate_via_const.clear
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_const.clear.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_const && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_const.clear.value & ~decoded_wr_biten[11:11]) | (decoded_wr_data[11:11] & decoded_wr_biten[11:11]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_const.clear.next = next_c;
        field_combo.saturate_via_const.clear.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_const.clear.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_const.clear.load_next) begin
                field_storage.saturate_via_const.clear.value <= field_combo.saturate_via_const.clear.next;
            end
        end
    end
    assign hwif_out.saturate_via_const.clear.value = field_storage.saturate_via_const.clear.value;
    // Field: regblock.saturate_via_const.set
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_const.set.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_const && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_const.set.value & ~decoded_wr_biten[12:12]) | (decoded_wr_data[12:12] & decoded_wr_biten[12:12]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_const.set.next = next_c;
        field_combo.saturate_via_const.set.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_const.set.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_const.set.load_next) begin
                field_storage.saturate_via_const.set.value <= field_combo.saturate_via_const.set.next;
            end
        end
    end
    assign hwif_out.saturate_via_const.set.value = field_storage.saturate_via_const.set.value;
    // Field: regblock.saturate_via_const.step
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_const.step.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_const && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_const.step.value & ~decoded_wr_biten[23:16]) | (decoded_wr_data[23:16] & decoded_wr_biten[23:16]);
            load_next_c = '1;
        end
        field_combo.saturate_via_const.step.next = next_c;
        field_combo.saturate_via_const.step.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_const.step.value <= 8'h1;
        end else begin
            if(field_combo.saturate_via_const.step.load_next) begin
                field_storage.saturate_via_const.step.value <= field_combo.saturate_via_const.step.next;
            end
        end
    end
    // Field: regblock.saturate_via_ref.count
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_ref.count.value;
        load_next_c = '0;
        if(field_storage.saturate_via_ref.set.value) begin // HW Set
            next_c = '1;
            load_next_c = '1;
        end else if(field_storage.saturate_via_ref.clear.value) begin // HW Clear
            next_c = '0;
            load_next_c = '1;
        end
        if(field_storage.saturate_via_ref.increment.value) begin // increment
            if(((9)'(next_c) + field_storage.saturate_via_ref.step.value) > field_storage.saturate_control.max.value) begin // up-counter saturated
                next_c = field_storage.saturate_control.max.value;
            end else begin
                next_c = next_c + field_storage.saturate_via_ref.step.value;
            end
            load_next_c = '1;
        end
        field_combo.saturate_via_ref.count.incrthreshold = (field_storage.saturate_via_ref.count.value >= 8'hff);
        field_combo.saturate_via_ref.count.incrsaturate = (field_storage.saturate_via_ref.count.value >= field_storage.saturate_control.max.value);
        if(field_storage.saturate_via_ref.decrement.value) begin // decrement
            if((9)'(next_c) < (field_storage.saturate_via_ref.step.value + field_storage.saturate_control.min.value)) begin // down-counter saturated
                next_c = field_storage.saturate_control.min.value;
            end else begin
                next_c = next_c - field_storage.saturate_via_ref.step.value;
            end
            load_next_c = '1;
        end
        field_combo.saturate_via_ref.count.decrthreshold = (field_storage.saturate_via_ref.count.value <= 8'd0);
        field_combo.saturate_via_ref.count.decrsaturate = (field_storage.saturate_via_ref.count.value <= field_storage.saturate_control.min.value);
        field_combo.saturate_via_ref.count.next = next_c;
        field_combo.saturate_via_ref.count.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_ref.count.value <= 8'h0;
        end else begin
            if(field_combo.saturate_via_ref.count.load_next) begin
                field_storage.saturate_via_ref.count.value <= field_combo.saturate_via_ref.count.next;
            end
        end
    end
    assign hwif_out.saturate_via_ref.count.value = field_storage.saturate_via_ref.count.value;
    // Field: regblock.saturate_via_ref.increment
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_ref.increment.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_ref && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_ref.increment.value & ~decoded_wr_biten[9:9]) | (decoded_wr_data[9:9] & decoded_wr_biten[9:9]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_ref.increment.next = next_c;
        field_combo.saturate_via_ref.increment.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_ref.increment.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_ref.increment.load_next) begin
                field_storage.saturate_via_ref.increment.value <= field_combo.saturate_via_ref.increment.next;
            end
        end
    end
    assign hwif_out.saturate_via_ref.increment.value = field_storage.saturate_via_ref.increment.value;
    // Field: regblock.saturate_via_ref.decrement
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_ref.decrement.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_ref && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_ref.decrement.value & ~decoded_wr_biten[10:10]) | (decoded_wr_data[10:10] & decoded_wr_biten[10:10]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_ref.decrement.next = next_c;
        field_combo.saturate_via_ref.decrement.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_ref.decrement.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_ref.decrement.load_next) begin
                field_storage.saturate_via_ref.decrement.value <= field_combo.saturate_via_ref.decrement.next;
            end
        end
    end
    assign hwif_out.saturate_via_ref.decrement.value = field_storage.saturate_via_ref.decrement.value;
    // Field: regblock.saturate_via_ref.clear
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_ref.clear.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_ref && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_ref.clear.value & ~decoded_wr_biten[11:11]) | (decoded_wr_data[11:11] & decoded_wr_biten[11:11]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_ref.clear.next = next_c;
        field_combo.saturate_via_ref.clear.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_ref.clear.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_ref.clear.load_next) begin
                field_storage.saturate_via_ref.clear.value <= field_combo.saturate_via_ref.clear.next;
            end
        end
    end
    assign hwif_out.saturate_via_ref.clear.value = field_storage.saturate_via_ref.clear.value;
    // Field: regblock.saturate_via_ref.set
    always_comb begin
        automatic logic [0:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_ref.set.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_ref && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_ref.set.value & ~decoded_wr_biten[12:12]) | (decoded_wr_data[12:12] & decoded_wr_biten[12:12]);
            load_next_c = '1;
        end else begin // singlepulse clears back to 0
            next_c = '0;
            load_next_c = '1;
        end
        field_combo.saturate_via_ref.set.next = next_c;
        field_combo.saturate_via_ref.set.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_ref.set.value <= 1'h0;
        end else begin
            if(field_combo.saturate_via_ref.set.load_next) begin
                field_storage.saturate_via_ref.set.value <= field_combo.saturate_via_ref.set.next;
            end
        end
    end
    assign hwif_out.saturate_via_ref.set.value = field_storage.saturate_via_ref.set.value;
    // Field: regblock.saturate_via_ref.step
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_via_ref.step.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_via_ref && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_via_ref.step.value & ~decoded_wr_biten[23:16]) | (decoded_wr_data[23:16] & decoded_wr_biten[23:16]);
            load_next_c = '1;
        end
        field_combo.saturate_via_ref.step.next = next_c;
        field_combo.saturate_via_ref.step.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_via_ref.step.value <= 8'h1;
        end else begin
            if(field_combo.saturate_via_ref.step.load_next) begin
                field_storage.saturate_via_ref.step.value <= field_combo.saturate_via_ref.step.next;
            end
        end
    end
    // Field: regblock.saturate_control.min
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_control.min.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_control && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_control.min.value & ~decoded_wr_biten[7:0]) | (decoded_wr_data[7:0] & decoded_wr_biten[7:0]);
            load_next_c = '1;
        end
        field_combo.saturate_control.min.next = next_c;
        field_combo.saturate_control.min.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_control.min.value <= 8'h0;
        end else begin
            if(field_combo.saturate_control.min.load_next) begin
                field_storage.saturate_control.min.value <= field_combo.saturate_control.min.next;
            end
        end
    end
    // Field: regblock.saturate_control.max
    always_comb begin
        automatic logic [7:0] next_c;
        automatic logic load_next_c;
        next_c = field_storage.saturate_control.max.value;
        load_next_c = '0;
        if(decoded_reg_strb.saturate_control && decoded_req_is_wr) begin // SW write
            next_c = (field_storage.saturate_control.max.value & ~decoded_wr_biten[15:8]) | (decoded_wr_data[15:8] & decoded_wr_biten[15:8]);
            load_next_c = '1;
        end
        field_combo.saturate_control.max.next = next_c;
        field_combo.saturate_control.max.load_next = load_next_c;
    end
    always_ff @(posedge clk) begin
        if(rst) begin
            field_storage.saturate_control.max.value <= 8'hff;
        end else begin
            if(field_combo.saturate_control.max.load_next) begin
                field_storage.saturate_control.max.value <= field_combo.saturate_control.max.next;
            end
        end
    end

    //--------------------------------------------------------------------------
    // Write response
    //--------------------------------------------------------------------------
    assign cpuif_wr_ack = decoded_req & decoded_req_is_wr;
    // Writes are always granted with no error response
    assign cpuif_wr_err = '0;

    //--------------------------------------------------------------------------
    // Readback
    //--------------------------------------------------------------------------

    logic readback_err;
    logic readback_done;
    logic [31:0] readback_data;

    // Assign readback values to a flattened array
    logic [31:0] readback_array[4];
    assign readback_array[0][7:0] = (decoded_reg_strb.saturate_via_bool && !decoded_req_is_wr) ? field_storage.saturate_via_bool.count.value : '0;
    assign readback_array[0][15:8] = '0;
    assign readback_array[0][23:16] = (decoded_reg_strb.saturate_via_bool && !decoded_req_is_wr) ? field_storage.saturate_via_bool.step.value : '0;
    assign readback_array[0][31:24] = '0;
    assign readback_array[1][7:0] = (decoded_reg_strb.saturate_via_const && !decoded_req_is_wr) ? field_storage.saturate_via_const.count.value : '0;
    assign readback_array[1][15:8] = '0;
    assign readback_array[1][23:16] = (decoded_reg_strb.saturate_via_const && !decoded_req_is_wr) ? field_storage.saturate_via_const.step.value : '0;
    assign readback_array[1][31:24] = '0;
    assign readback_array[2][7:0] = (decoded_reg_strb.saturate_via_ref && !decoded_req_is_wr) ? field_storage.saturate_via_ref.count.value : '0;
    assign readback_array[2][15:8] = '0;
    assign readback_array[2][23:16] = (decoded_reg_strb.saturate_via_ref && !decoded_req_is_wr) ? field_storage.saturate_via_ref.step.value : '0;
    assign readback_array[2][31:24] = '0;
    assign readback_array[3][7:0] = (decoded_reg_strb.saturate_control && !decoded_req_is_wr) ? field_storage.saturate_control.min.value : '0;
    assign readback_array[3][15:8] = (decoded_reg_strb.saturate_control && !decoded_req_is_wr) ? field_storage.saturate_control.max.value : '0;
    assign readback_array[3][31:16] = '0;

    // Reduce the array
    always_comb begin
        automatic logic [31:0] readback_data_var;
        readback_done = decoded_req & ~decoded_req_is_wr;
        readback_err = '0;
        readback_data_var = '0;
        for(int i=0; i<4; i++) readback_data_var |= readback_array[i];
        readback_data = readback_data_var;
    end

    assign cpuif_rd_ack = readback_done;
    assign cpuif_rd_data = readback_data;
    assign cpuif_rd_err = readback_err;
endmodule
