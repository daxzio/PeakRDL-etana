// Generated by PeakRDL-etana - A free and open-source SystemVerilog generator
//  https://github.com/daxzio/PeakRDL-etana

module regblock (
        input wire clk,
        input wire rst,

        input wire s_apb_psel,
        input wire s_apb_pwrite,
        input wire s_apb_penable,
        input wire [2:0] s_apb_pprot,
        input wire [11:0] s_apb_paddr,
        input wire [31:0] s_apb_pwdata,
        input wire [3:0] s_apb_pstrb,
        output logic s_apb_pready,
        output logic [31:0] s_apb_prdata,
        output logic s_apb_pslverr


    );

    //--------------------------------------------------------------------------
    // CPU Bus interface logic
    //--------------------------------------------------------------------------
    logic cpuif_req;
    logic cpuif_req_is_wr;
    logic [11:0] cpuif_addr;
    logic [31:0] cpuif_wr_data;
    logic [31:0] cpuif_wr_biten;
    logic cpuif_req_stall_wr;
    logic cpuif_req_stall_rd;

    logic cpuif_rd_ack;
    logic cpuif_rd_err;
    logic [31:0] cpuif_rd_data;

    logic cpuif_wr_ack;
    logic cpuif_wr_err;

    logic [9:0] addr ;
    assign addr = cpuif_addr[11:2];

    // Request
    logic is_active;
    always_ff @(posedge clk) begin
        if(rst) begin
            is_active <= '0;
            cpuif_req <= '0;
            cpuif_req_is_wr <= '0;
            cpuif_addr <= '0;
            cpuif_wr_data <= '0;
            cpuif_wr_biten <= '0;
        end else begin
            if(~is_active) begin
                if(s_apb_psel) begin
                    is_active <= '1;
                    cpuif_req <= '1;
                    cpuif_req_is_wr <= s_apb_pwrite;
                    cpuif_addr <= {s_apb_paddr[11:2], 2'b0};
                    cpuif_wr_data <= s_apb_pwdata;
                    for(int i=0; i<4; i++) begin
                        cpuif_wr_biten[i*8 +: 8] <= {8{s_apb_pstrb[i]}};
                    end
                end
            end else begin
                cpuif_req <= '0;
                if(cpuif_rd_ack || cpuif_wr_ack) begin
                    is_active <= '0;
                end
            end
        end
    end

    // Response
    assign s_apb_pready = cpuif_rd_ack | cpuif_wr_ack;
    assign s_apb_prdata = cpuif_rd_data;
    assign s_apb_pslverr = cpuif_rd_err | cpuif_wr_err;

    logic cpuif_req_masked;

    // Read & write latencies are balanced. Stalls not required
    assign cpuif_req_stall_rd = '0;
    assign cpuif_req_stall_wr = '0;
    assign cpuif_req_masked = cpuif_req
                            & !(!cpuif_req_is_wr & cpuif_req_stall_rd)
                            & !(cpuif_req_is_wr & cpuif_req_stall_wr);

    //--------------------------------------------------------------------------
    // Address Decode
    //--------------------------------------------------------------------------
    logic [1023:0] decoded_reg_strb_r1;
    logic decoded_req;
    logic decoded_req_is_wr;
    /* verilator lint_off UNUSEDSIGNAL */
    logic [31:0] decoded_wr_data;
    logic [31:0] decoded_wr_biten;
    /* verilator lint_on UNUSEDSIGNAL */


    always @(*) begin
        /* verilator lint_off UNUSEDSIGNAL */
        integer next_cpuif_addr;
        /* verilator lint_on UNUSEDSIGNAL */
        decoded_reg_strb_r1 = 0;
        if (cpuif_req_masked) decoded_reg_strb_r1[addr] = 1;
//         for(int i0=0; i0<1024; i0++) begin : gen_loop_3
//             next_cpuif_addr = 32'h0 + i0*12'h4;
//             decoded_reg_strb_r1[i0] = cpuif_req_masked & (cpuif_addr == next_cpuif_addr[11:0]);
//         end
    end

    // Pass down signals to next stage
    assign decoded_req = cpuif_req_masked;
    assign decoded_req_is_wr = cpuif_req_is_wr;
    assign decoded_wr_data = cpuif_wr_data;
    assign decoded_wr_biten = cpuif_wr_biten;


    //--------------------------------------------------------------------------
    // Field storage declarations
    //--------------------------------------------------------------------------

    // Field: regblock.r1[].f
//     logic [31:0] field_storage_r1_f_value [1024] ;
//     logic [31:0] field_combo_r1_f_next [1024] ;
    logic [32767:0] field_storage_r1_f_value ;
    logic [32767:0] field_combo_r1_f_next ;
    logic [1023:0] field_combo_r1_f_load_next ;
    //--------------------------------------------------------------------------
    // Field logic
    //--------------------------------------------------------------------------
//     for(genvar i0=0; i0<1024; i0++) begin : gen_loop_5
//         always @(*) begin
//             logic [31:0] next_c;
//             logic load_next_c;
//             next_c = field_storage_r1_f_value[i0*32+:32];
//             load_next_c = '0;
//             if(decoded_reg_strb_r1[i0] && decoded_req_is_wr) begin // SW write
//                 next_c = (field_storage_r1_f_value[i0*32+:32] & ~decoded_wr_biten[31:0]) | (decoded_wr_data[31:0] & decoded_wr_biten[31:0]);
//                 load_next_c = '1;
//             end
//             field_combo_r1_f_next[i0*32+:32] = next_c;
//             field_combo_r1_f_load_next[i0] = load_next_c;
//         end
//     end
    always @(*) begin
        field_combo_r1_f_next = field_storage_r1_f_value;
        field_combo_r1_f_load_next = 0;
//         for(int i0=0; i0<1024; i0++) begin : gen_loop_yyy1
            if(decoded_reg_strb_r1[addr] && decoded_req_is_wr) begin // SW write
                field_combo_r1_f_next[addr*32+:32] = (field_storage_r1_f_value[addr*32+:32] & ~decoded_wr_biten[31:0]) | (decoded_wr_data[31:0] & decoded_wr_biten[31:0]);
                field_combo_r1_f_load_next[addr] = 1;
            end
//         end
    end
//     for(genvar i0=0; i0<1024; i0++) begin : gen_loop_xxx
//         always_ff @(posedge clk) begin
//             if(rst) begin
//                 field_storage_r1_f_value[i0*32+:32] <= 32'h11;
//             end else if(field_combo_r1_f_load_next[i0]) begin
//                 field_storage_r1_f_value[i0*32+:32] <= field_combo_r1_f_next[i0*32+:32];
//             end
//         end
//     end
        always_ff @(posedge clk) begin
            for(int i0=0; i0<1024; i0++) begin : gen_loop_xxx1
                if(rst) begin
                    field_storage_r1_f_value[i0*32+:32] <= 32'h11;
                end else if(field_combo_r1_f_load_next[i0]) begin
                    field_storage_r1_f_value[i0*32+:32] <= field_combo_r1_f_next[i0*32+:32];
                end
            end
        end
    //--------------------------------------------------------------------------
    // Write response
    //--------------------------------------------------------------------------
    assign cpuif_wr_ack = decoded_req & decoded_req_is_wr;
    // Writes are always granted with no error response
    assign cpuif_wr_err = '0;

//--------------------------------------------------------------------------
// Readback
//--------------------------------------------------------------------------
    logic readback_external_rd_ack;
    assign readback_external_rd_ack = 0;

    logic readback_err;
    logic readback_done;
    logic [31:0] readback_data;

    logic [31:0] readback_array[1024];
    for(genvar i0=0; i0<1024; i0++) begin : gen_loop_1
//         assign readback_array[i0*1 + 0][31:0] = (decoded_reg_strb_r1[i0] && !decoded_req_is_wr) ? field_storage_r1_f_value[i0*32+:32] : '0;
        assign readback_array[i0*1 + 0][31:0] = field_storage_r1_f_value[i0*32+:32];
    end
//     // Assign readback values to a flattened array
//     logic [32767:0] readback_array;
//     for(genvar i0=0; i0<1024; i0++) begin : gen_loop_1
//         //assign readback_array[i0*32+:32] = (decoded_reg_strb_r1[i0] && !decoded_req_is_wr) ? field_storage_r1_f_value[i0*32+:32] : '0;
//         assign readback_array[i0*32+:32] = field_storage_r1_f_value[i0*32+:32];
//     end

    // Readback data mux
    always @(*) begin
        readback_done = decoded_req & ~decoded_req_is_wr;
        readback_err = '0;
        readback_data = '0;
        if (readback_done) begin
//             readback_data = field_storage_r1_f_value[addr*32+:32];
//             readback_data = readback_array[addr*32+:32];
            readback_data = readback_array[addr];
        end
    end


    assign cpuif_rd_ack = readback_done;
    assign cpuif_rd_data = readback_data;
    assign cpuif_rd_err = readback_err;
endmodule
